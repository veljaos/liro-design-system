---
'@veljaos/tokens': patch
'@veljaos/ui': patch
---

P2.2a: `Field`, `TextField`, `TextAreaField`, `SelectField`, `CheckboxField`, `SwitchField` and `RadioGroupField`, with the error under the field, a required mark, read-only as plain text and disabled with a visible reason. `@veljaos/tokens` adds the `border-status-danger-fg` utility for the border of an invalid field.
